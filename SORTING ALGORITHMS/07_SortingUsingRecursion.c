// HARSHIT YADAV
// 2105891
//  Sorting using recursion
#include <stdio.h>
#include <stdlib.h>

int Sort(int ar[], int size)
{
    int i, pos;
    int small = ar[0];
    while (i < size)
    {
        if (ar[i] < small)
        {
            small = ar[i];
            pos = i;
        }
    }
    if (pos+1 == size)
    return;

    else{
        temp
    }
}

int main()
{
    return 0;
}